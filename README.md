# keys
get the keys from object

###Usage
```javascript
'use strict';

let keys = require('getkeys');
let obj = {
    name : 'krew',
    sex : 'male'
}

console.log(keys(obj))    //['name','sex']

```